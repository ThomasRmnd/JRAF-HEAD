[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "jrafhead"
version = "0.1.0"
description = "JUNO Reactor Analysis Framework"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["jrafhead"]